# Commit Log

## 2026-03-11 - 提交记录 1
- 修改内容:
  - 调整 `.gitignore`，默认忽略 `storage/*` 与 `sessions/*`，仅保留 `storage/testdata/**` 与 `sessions/test_logs/**`。
  - 新增测试样例数据 `storage/testdata/demo.csv`。
  - 新增测试样例日志 `sessions/test_logs/demo_records.jsonl`。
  - 更新 `services/tool_backend_service/main.py`，工具包 endpoint 由硬编码本机地址改为基于项目地址动态生成（支持 `TOOL_BACKEND_PUBLIC_URL`）。
  - 更新 `services/frontend_service/main.py`，`/health` 不再暴露内部服务本机地址。
  - 更新 `README.md`，移除本机绝对路径/本机地址示例，改为 `<PROJECT_ROOT>` 与 `<PROJECT_URL>`。
- 修改原因:
  - 避免把用户真实数据集和运行记录上传到仓库。
  - 避免在代码与文档中暴露本机地址信息。
  - 统一以项目地址作为对外初始地址。
- 作用:
  - 仓库可保留可复现的测试数据与测试日志，同时自动屏蔽用户数据。
  - 部署和协作时不依赖个人机器路径，减少敏感信息泄露风险。
  - 远程工具安装后默认 endpoint 更通用，适配不同环境域名。

## 2026-03-11 - 提交记录 2
- 修改内容:
  - 新增 `docs/process.md`，定义项目标准迭代流程与强制执行规则。
- 修改原因:
  - 明确后续迭代的统一流程，避免需求、开发、测试、验收阶段缺失。
- 作用:
  - 为后续每次迭代提供标准执行路径与留痕依据，提升可追踪性与交付稳定性。

## 2026-03-11 - 提交记录 3
- 修改内容:
  - 新增销售主题自动分析能力：`/insights/auto`。
  - 新增商业报告导出能力：`/insights/export`，支持 Markdown/PDF。
  - 新增独立商业建议页面：`/insights`（`insights.html` + `insights.js`）。
  - 首页增加“商业建议”入口。
  - 新增测试 `tests/test_insights.py`。
  - 新增 ITER-001 留痕文档：需求、验证清单、测试报告、迭代总结。
  - README 增加商业建议页与新接口说明。
- 修改原因:
  - 面向商业用户提供无需手工提问的一键自动分析能力。
  - 满足“销售主题优先、新页面展示、支持 Markdown/PDF 导出”的确认需求。
- 作用:
  - 商业用户可直接获得结构化建议（发现/证据/动作/优先级/影响预估）。
  - 输出可直接用于业务评审与汇报，提升分析落地效率。
  - 为后续扩展运营/财务/客户主题打下统一接口与页面基础。

## 2026-03-11 - 提交记录 4
- 修改内容:
  - 商业分析页新增主题下拉单选（销售/运营/财务/客户）。
  - 新增“具体分析需求”输入框，并接入 `/insights/auto` 与 `/insights/export`。
  - 后端新增 `auto_business_insights`，支持主题参数与需求参数。
  - 报告结果区支持滚动并新增图表展示（Top客户柱状图、趋势折线图）。
  - 修复 PDF 导出乱码：使用 CJK 字体映射生成可读中文 PDF。
- 修改原因:
  - 响应用户验收反馈，补齐多主题选择、需求驱动分析和可读导出能力。
- 作用:
  - 用户可以按主题和具体目标分析数据，输出更贴近业务场景。
  - 页面可阅读性与报告可交付性显著提升。

## 2026-03-11 - 提交记录 5
- 修改内容:
  - 新增 `docs/iterations/ITER-002/` 迭代文档骨架：
    - `requirement.md`
    - `validation-checklist.md`
    - `test-report.md`
    - `summary.md`
    - `status.md`
- 修改原因:
  - 按项目标准流程重新启动新一轮迭代，并将“需求确认前”的材料先行落地。
- 作用:
  - 形成可执行、可追踪的 ITER-002 起点，便于需求确认后直接进入开发与测试阶段。

## 2026-03-11 - 提交记录 6
- 修改内容:
  - 修复 `ITER-001` 导出报告图片未嵌入问题：
    - Markdown 导出改为内嵌 data URI 图片。
    - PDF 导出改为附加图表页并嵌入实际图片。
  - 新增/更新问题台账机制：
    - 新增 `docs/iterations/ITER-001/issues.md` 并记录该问题为 `已解决`。
    - 新增 `docs/iterations/ITER-002/issues.md` 作为后续台账模板。
    - 更新 `docs/process.md`，将 `issues.md` 纳入每次迭代强制留痕项。
  - 更新测试 `tests/test_insights.py`，校验导出结果包含嵌入图片能力。
- 修改原因:
  - 用户反馈导出文件仅有图片地址，不满足可直接交付要求。
  - 需要建立统一的问题留痕与状态管理，便于后续迭代续接。
- 作用:
  - 导出报告可直接携带图表内容，提升可读性和可交付性。
  - 迭代问题可按 `已解决/未解决` 快速追踪，降低信息断层风险。

## 2026-03-11 - 提交记录 7
- 修改内容:
  - 升级非销售主题自动分析实现：`app/agent.py`
    - 运营主题：新增状态分布、吞吐趋势、时效/成本建议逻辑与图表输出。
    - 财务主题：新增收支利润、月度波动、收入集中度建议逻辑与图表输出。
    - 客户主题：新增客户规模、价值集中度、活跃趋势/流失/分层建议逻辑与图表输出。
  - 新增测试 `tests/test_insights.py::test_auto_business_insights_topic_specific_semantics`，校验三类主题语义与表格命名。
  - 更新 `docs/iterations/ITER-002/` 留痕文档（状态、测试报告、总结、问题台账）。
- 修改原因:
  - `ITER-002` 未完成项中，非销售主题此前仅做销售结果文案改写，不满足“主题能力扩展”的真实目标。
- 作用:
  - 多主题输出从“同构文案”升级为“主题化分析”，可读性和业务可执行性显著提升。
  - 迭代文档与测试记录同步闭环，便于后续用户验收与合并。
